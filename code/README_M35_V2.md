# M3.5 v2
Adds an explicit SAME_ENTITY jurisdiction separation rule and a six-case regression harness.

Copy `code/adjudication/` and `code/run_resolution_tests.py` into the project.

Run:
```bash
python3 ./code/run_resolution_tests.py
```

Expected: `6 passed / 0 failed`.
